(env
  (dev
    (flags (:standard -warn-error -A -g ))))

(executable
  (name		testje)
  (public_name	testje)
  (package	tlsping)
  (modules	testje)
  (libraries	cmdliner hex socks socks-lwt tlsping unix)
)
